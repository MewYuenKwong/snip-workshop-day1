# Snip Backend

A tiny URL shortener backend built with Bun and a single `server.js` file.

## Run

```bash
bun install
bun start
```

The server listens on port 3000 by default and stores links in memory.

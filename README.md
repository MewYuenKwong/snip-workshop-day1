# Snip CLI

A zero-dependency Node CLI for the Snip backend.

## Usage

```bash
snip add https://example.com
snip ls
snip open ABC123
snip help
```

The default backend URL is `http://localhost:3000`, or override it with `SNIP_API`.
